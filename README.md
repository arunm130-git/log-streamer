# log-streamer
Log Streamer is a Symfony-based PHP app that reads, parses, and stores log data in a MySQL database. It exposes an API to filter and count log entries based on various criteria. Dockerized for easy setup, ideal for real-time log processing and analysis.
